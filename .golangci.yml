linters:
  presets:
    - bugs
    - comment
    - error
    - format
    - import
    - metalinter
    - module
    - performance
    - style
    - test
    - unused
    # Disable
    # - sql
    # - complexity
  disable:
    - ireturn
    - wsl
    - gomnd
    - depguard
    - mnd
    # - exhaustruct
    - gci
  enable:
    - gosimple
    - unconvert
linters-settings:
  stylecheck:
    dot-import-whitelist:
      - "github.com/malgo-lang/malgo/pretty"
  revive:
    rules:
      - name: dot-imports
        disabled: true
  godot:
    exclude:
      - ;$
  forbidigo:
    forbid:
      - ^print.*$
  varnamelen:
    ignore-decls:
      - i int
      - ok bool
      - w io.Writer
      - o *PrettyOpts