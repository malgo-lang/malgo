module Language.Malgo.MiddleEnd.TransToGraph ( TransToGraph ) where

-- import Language.Malgo.Prelude
-- import Language.Malgo.Monad
-- import Language.Malgo.Pass
-- import Language.Malgo.TypeRep.LType as L
-- import Language.Malgo.ID
-- import qualified Language.Malgo.IR.LIR as L
-- import Language.Malgo.IR.Graph as G

-- import Compiler.Hoopl

data TransToGraph

-- instance Pass TransToGraph (L.Program (ID LType)) Program where
--   passName = "TransToGraph"
--   isDump = const False
--   trans L.Program { L.functions, L.mainFunc } = undefined

-- transFunc L.Func { L.name, L.params, L.body } = do
--   (entry, body') <- transBlock body
--   pure $ Func { name, params, entry, body = body' }

-- transBlock = undefined
  