def printer.0($def.2)
  cocase
    .print(; $object.10) ->
      cocase
        .ap($p1.4; $lambda.3) ->
          μ $case.5:
            μ $ap.6:
              :tuple
              | .ap($p1.4; $ap.6)
            | case
                :tuple.ap(x.7; $_.8) ->
                  μ $prim.9:
                    prim print(x.7; $prim.9)
                  | $case.5
          | $lambda.3
      | $object.10
  | $def.2
def main.1($def.11)
  cocase
    .ap(; $lambda.12) ->
      μ $ap.15:
        μ $proj.13:
          μ $top.14:
            invoke printer.0($top.14)
          | .print(; $proj.13)
        | .ap(1; $ap.15)
      | $lambda.12
  | $def.11
