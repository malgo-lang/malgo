-- -*- tab-width: 2 -*-

id x = x;

-- recursive function
fib n =
  if n <= 1
  then 1
  else let a = fib (n - 1) in
       let b = fib (n - 2) in
       a + b;

-- entry point
main : Array String -> Answer;
main argv =
  let f = id id
  in println (intToString ((id fib) 30)) exit;
