module Test4 = {
  module {..} = import Builtin;

  infixl 6 (+#);
  (+#) = {x y -> addInt64# x y};

  infixl 6 (+);
  (+) = {(Int64# x) (Int64# y) -> Int64# (x +# y) };

  data List a = Nil | Cons a (List a);

  sum = { acc Nil -> acc 
        | acc (Cons x xs) -> sum (acc + x) xs };

  main = {
    {(Int64# i) -> printString# (toStringInt64# i) }
      (sum (Int64# 0L#) (Cons (Int64# 1L#) (Cons (Int64# 2L#) Nil)))
    };
}
-- Expected: 3