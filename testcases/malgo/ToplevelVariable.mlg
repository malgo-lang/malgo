module ToplevelVariable = {
  module {..} = import Builtin;
  module {..} = import Prelude;

  one = 1;

  addOne = addInt32 one;

  constId = const identity;

  comp : Maybe a;
  comp = { Nothing -> Nothing
         | Just x -> Nothing
         } (Just ());

  main = {
    { Nothing -> printString "OK"
    | Just _ -> ()
    } comp;
    printInt32 (constId const (addOne one))
  };
}

-- Expected: OK2
