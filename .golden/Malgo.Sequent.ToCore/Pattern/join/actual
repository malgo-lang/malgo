((main
    $Pattern.return_xxx
    (cut
       (lambda
          ($Pattern.param_xxx $Pattern.return_xxx)
          (join
             $Pattern.select_xxx
             (select
                (#Pattern.$__xxx
                   (join
                      $Pattern.then_xxx
                      (then
                         $Pattern.outer_xxx
                         (join
                            $Pattern.return_xxx
                            (then
                               $Pattern.inner_xxx
                               (join
                                  $Pattern.apply_xxx
                                  (apply
                                     ($Pattern.inner_xxx)
                                     ($Pattern.return_xxx))
                                  (cut $Pattern.outer_xxx $Pattern.apply_xxx)))
                            (join
                               $Pattern.apply_xxx
                               (apply ("OK") ($Pattern.return_xxx))
                               (invoke String# $Pattern.apply_xxx))))
                      (invoke putStrLn $Pattern.then_xxx))))
             (cut $Pattern.param_xxx $Pattern.select_xxx)))
       $Pattern.return_xxx))
   (isNil
      $Pattern.return_xxx
      (cut
         (lambda
            ($Pattern.param_xxx $Pattern.return_xxx)
            (join
               $Pattern.select_xxx
               (select
                  ((Nil ()) (invoke True $Pattern.return_xxx))
                  (#Pattern.__xxx (invoke False $Pattern.return_xxx)))
               (cut $Pattern.param_xxx $Pattern.select_xxx)))
         $Pattern.return_xxx)))