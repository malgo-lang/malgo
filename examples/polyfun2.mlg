let fun f(p) = match p with
               | { k, x } -> k(x) in
let fun id(x) = x in
let fun addOne(x) = x + 1 in
let val funRef0 = array({id, 1}, 1) in
let val funRef1 = array({addOne, 1}, 1) in
let extern print_int : Int -> {} = "print_int" in
print_int(f(funRef0[0]));
print_int(f(funRef1[0]))