(define (fizzbuzz:string n:int)
  (if (= (mod n 15) 0)
      "FizzBuzz"
      (if (= (mod n 3) 0)
          "Fizz"
          (if (= (mod n 5) 0)
              "Buzz"
              (int-to-string n)))))

(define (main:int)
  (for (n (iota 1 100))
    (println (fizzbuzz n))))
