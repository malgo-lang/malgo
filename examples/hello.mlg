extern println(str:String):Unit
extern print(str:String):Unit
extern print_int(i:Int):Unit

def fib(n:Int):Int = {
  if (n <= 0 || n == 1) && #t {
    1
  } else {
    fib(n-1) + fib(n-2)
  }
}

def vartest(i:Int, f:Float):String = {
    let i:String = "hoge";
    let a:Int = 42;
    {
      let b:Float = f;
      print_int(a);
    };
    i
}

def main():Unit = {
  println("Hello, world!");
  print("fib(10) = ");
  print_int(fib(10));
  println("");
}