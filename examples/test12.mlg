let fun even(n:Int):Bool =
      if n == 0
      then true
      else odd(n - 1)
    fun odd(n:Int):Bool =
      if n == 0
      then false
      else even(n - 1)
    extern println_bool : Bool -> {} = "println_bool"
in println_bool(odd(34))
end