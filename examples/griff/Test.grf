infixl 0 (|>);
(|>) :: a -> (a -> b) -> b;
(|>) x f = f x;

data RGB = Red | Green | Blue;

rTob = {Red -> Blue | a -> a};

data Bool = True | False;

if = {True t _ -> t! | False _ f -> f!};

forign import print_int :: Int64# -> ();

data Int = Int# Int64#;

-- main = {if True {False} {True}};
main = {
  Int# 1L# |> {(Int# i) -> print_int i}
}

