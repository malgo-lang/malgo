module Record = {
  module {..} = import Builtin;
  module {..} = import Prelude;

  type Person = { name: String, age: Int32 };

  personAge = { { name = _, age = age } -> age };

  main = {
    personAge {name = "Hoge", age = 42}
      |> toStringInt32
      |> putStrLn
  };
}

-- Expected: 42
