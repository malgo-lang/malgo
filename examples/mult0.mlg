def mult(l) = label a multAux(l; a)

data List(t) = Nil() | Cons(t, List(t))

def multAux(l; a) = l match {
    Nil() -> 1,
    Cons(x, xs) -> x switch {
        0 -> 0 goto a,
        default -> prim(mul, x, multAux(xs; a)),
    },
}

def main() = mult(Cons(1, Cons(0, Cons(3, Nil()))))