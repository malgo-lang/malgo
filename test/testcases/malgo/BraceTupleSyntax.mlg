module {..} = import "../../../runtime/malgo/Builtin.mlg"

data T = A | B | C

def main : { () }
def main = {
  {A, B}
    |> { {A, rest} -> printString "brace tuple works",
         {B, rest} -> (),
         {C, rest} -> ()
       }
}