module {..} = import "../../runtime/malgo/Builtin.mlg"

def testTupleExpr = {
  x = {1, 2, 3},
  y = {"hello", "world"},
  z = {x, y}
}

def testTuplePattern = {
  {x, y} -> x + y,
  {a, b, c} -> a + b + c
}

def main = {
  putStrLn "Brace tuple syntax test"
}