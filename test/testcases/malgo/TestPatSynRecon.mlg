module {..} = import "../../../runtime/malgo/Builtin.mlg"
module {..} = import "../../../runtime/malgo/Prelude.mlg"

infixl 6 (+#)
def (+#) = {x y -> addInt64# x y}

def sum =
  { (Cons (Int64# x) xs) ->
      sum xs |> {(Int64# s) -> Int64# (x +# s)},
    Nil -> Int64# 0i64#
  }

def main = {
  sum (Cons (Int64# 1i64#) (Cons (Int64# 2i64#) Nil))
    |> {i -> printInt64 i}
}
